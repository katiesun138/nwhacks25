import React from 'react';
import ReactDOM from 'react-dom';
import BlockingPage from './BlockingPage'; // Make sure the import path is correct

ReactDOM.render(
  <React.StrictMode>
    <BlockingPage />
  </React.StrictMode>,
  document.getElementById('root')
);
